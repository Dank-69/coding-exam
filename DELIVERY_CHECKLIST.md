# 当前项目交付清单

## 1. 已具备

以下内容当前仓库已经具备，可以直接作为交付物的一部分：

- 代码工程：Spring Boot + Maven 项目完整可运行
- 三阶段页面与接口：
  - `v1` 关键词搜索
  - `v2` 语义搜索
  - `v3` Agent 对话
- 数据准备：`data/*.html`
- Phase 3 结构化 SOP 文件：
  - `data/sop_index.json`
  - `data/sop-xxx/index.json`
  - `data/sop-xxx/*.md`
- 项目文档：
  - `README.md`
  - `DESIGN_DOC.md`
  - `DELIVERY_CHECKLIST.md`
- 自动化测试
- 本地回归结果：`mvn -q test` 已通过

## 2. 提交前必须补齐

这部分是当前仓库还没有看到、但最终交付大概率需要补上的内容：

1. `PROMPTS.md`
   记录你在完成题目过程中使用过的关键 prompt、AI 辅助方式、阶段性策略。

2. 页面与接口截图
   建议至少准备：
   - `/v1` 页面和搜索结果
   - `/v2` 页面和语义检索结果
   - `/v3` 页面、tool trace、最终回答
   - 关键接口返回截图

3. 简历文件
   如果面试要求代码包同时附带简历，需要把简历一并放入最终交付包。

4. 最终提交包说明
   最好补一个简短的提交说明，写清：
   - 启动命令
   - 需要的环境变量
   - 哪些能力可离线演示
   - 哪些能力在配置 API Key 后效果更完整

## 3. 交付前必须再检查一次

1. 不要提交真实密钥
   当前我已经把 `application.yml` 默认值改成空占位；最终提交前仍建议再全仓搜索一次 `sk-`、`api-key`、`token`。

2. 新环境可启动
   至少执行一次：

   ```bash
   mvn -q test
   mvn spring-boot:run
   ```

3. README 与代码一致
   特别确认：
   - `POST /v3/chat/stream` 路径
   - `POST /v3/chat` 请求结构
   - 环境变量命名

4. 截图与当前界面一致
   不要混用旧版本界面截图。

## 4. 建议补强

这部分不是硬性缺失，但补上会让交付更完整：

1. 增加一个 `screenshots/` 目录统一存放验收截图。
2. 增加一个 `PROMPTS.md` 模板，按 Phase 1/2/3 分段记录。
3. 增加一次“无 API Key 也可演示”的说明，降低评审环境不一致风险。
4. 如果时间允许，补一页“已知局限”说明，提前管理预期。
