# 五、禁止操作

以下操作在值班期间严格禁止：一、在业务高峰期执行ALTER TABLE等DDL操作；二、未经备份直接删除或修改生产数据；三、在主库上执行不带WHERE条件的UPDATE或DELETE语句；四、手动修改数据库的系统参数后不记录变更；五、直接在生产实例上执行全表扫描的查询；六、未经确认直接进行主从切换；七、使用root账号执行日常运维操作。所有线上操作必须先在预发布环境验证，且需记录完整的操作日志。
