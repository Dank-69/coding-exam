# 三、常见故障处理索引

- 场景一：主从复制中断：文件 `sop-002/03_01_replication_interruption.md`；关键词：p0、p1、k8s、主从、ab、SHOW、SLAVE、STATUS、Binlog、DDL、SQL、GTID、SET、GTID_NEXT；摘要：发现主从复制中断时，首先通过SHOW SLAVE STATUS命令检查复制错误信息。常见原因包括：主库Binlog被清理导致从库找不到对应位点、DDL语句在从库执行失败、主键冲突。如果是Binl...
- 场景二：数据库连接数暴涨：文件 `sop-002/03_02_db_connections_spike.md`；关键词：p0、p1、异常、数据库、连接池、SHOW、PROCESSLIST、Kill、max_connections、ProxySQL；摘要：当数据库连接数接近上限时，先通过SHOW PROCESSLIST查看当前活跃连接的分布情况，按用户、主机、命令类型统计。如果是某个应用实例异常创建大量连接，通知该应用负责人并考虑临时Kill该来...
- 场景三：磁盘空间告急：文件 `sop-002/03_03_disk_space.md`；关键词：p0、p1、异常、处理、慢查询、ab、Binlog、expire_logs_days、PURGE、BINARY、LOGS、InnoDB、OPTIMIZE、TABLE；摘要：磁盘空间使用率超过百分之八十五时需要立即处理。首先确认空间占用来源：数据文件、Binlog、慢查询日志、临时文件。如果是Binlog占用过大，检查expire_logs_days设置，必要时手动...
- 场景四：大事务阻塞：文件 `sop-002/03_04_large_transaction_blocking.md`；关键词：沟通、回滚、information_schema、innodb_trx、SHOW、ENGINE、INNODB、STATUS、Kill、ETL；摘要：当检测到长时间运行的大事务导致锁等待链过长时，首先通过information_schema的innodb_trx表查看当前活跃事务的持续时间和锁信息。使用SHOW ENGINE INNODB S...
- 场景五：Redis内存溢出：文件 `sop-002/03_05_redis_oom.md`；关键词：redis、Redis、maxmemory、noeviction、INFO、MEMORY、DOCTOR、redis-cli、bigkeys、hotkeys、Key、Hash、List；摘要：Redis实例内存使用达到maxmemory限制时，根据淘汰策略的不同表现也不同。如果配置了noeviction策略，写入操作会直接报错。首先通过Redis的INFO MEMORY命令和MEMO...
