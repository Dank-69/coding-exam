# 三、常见故障处理索引

- 场景一：白屏/页面无法加载：文件 `sop-003/03_01_blank_page.md`；关键词：p0、p1、异常、回滚、监控、指标、白屏、js、cdn、dns、崩溃、cd、HTML、CDN；摘要：用户反馈页面白屏时,首先确认影响范围。通过监控平台查看白屏率指标,判断是全量用户还是部分用户受影响。如果是全量白屏,检查HTML主文档是否正常返回,CDN是否异常,DNS解析是否正确。如果是部分...
- 场景二：JS错误率突增：文件 `sop-003/03_02_js_errors.md`；关键词：p0、p1、告警、js、JS、Sentry、API、Script、error、CORS、ChunkLoadError；摘要：Sentry告警显示JS错误数量突增时,首先在Sentry中查看新增错误的堆栈信息和影响用户数。按浏览器版本、操作系统、页面路径等维度分析错误分布。如果错误集中在某个浏览器版本,可能是兼容性问题...
- 场景三：页面加载性能下降：文件 `sop-003/03_03_page_performance.md`；关键词：p0、p1、响应、排查、监控、指标、延迟、js、LCP、WebP、CSS、JS、Lighthouse、Chrome；摘要：当LCP指标超过阈值时,通过监控平台分析性能下降的时间节点和影响范围。常见原因：一、打包体积过大,新功能引入了大量第三方库;二、图片未优化,大尺寸图片未使用WebP格式或未做懒加载;三、关键渲染...
- 场景四：样式错乱/布局异常：文件 `sop-003/03_04_layout_issue.md`；关键词：p0、p1、异常、响应、js、cdn、推送、cd、CSS、Grid、IE、viewport、CSS-in-JS、CDN；摘要：收到样式错乱的反馈时,首先确认受影响的页面、浏览器和设备型号。检查CSS文件是否正常加载,是否存在CSS文件404的情况。如果是特定浏览器的兼容性问题,检查是否使用了该浏览器不支持的CSS属性,...
- 场景五：接口跨域(CORS)错误：文件 `sop-003/03_05_cors_error.md`；关键词：p0、p1、响应、处理、CORS、Access-Control-Allow-Origin、OPTIONS、Nginx、Cookie、Access-Control-Allow-Credentials、true、Allow-Origin；摘要：浏览器控制台出现CORS错误时,首先确认是哪个接口触发了跨域限制。检查接口服务的Access-Control-Allow-Origin响应头配置是否包含当前域名。如果是预检请求(OPTIONS)...
