# 三、常见故障处理索引

- 场景一：离线任务大面积失败：文件 `sop-006/03_01_offline_jobs_failure.md`；关键词：p0、p1、异常、安全、Hadoop、YARN、ResourceManager、Web、UI、Container、HDFS、NameNode、Hive、Metastore；摘要：调度系统显示大量离线任务失败时，首先检查是否是Hadoop集群层面的问题。查看YARN ResourceManager的Web UI确认集群状态，检查是否有大量Container启动失败。常见原...
- 场景二：Flink实时任务异常：文件 `sop-006/03_02_flink_job_failure.md`；关键词：p0、p1、异常、处理、恢复、延迟、Flink、Checkpoint、Dashboard、Sink、SubTask、State、Key、Savepoint；摘要：Flink任务出现消费延迟持续增长或Checkpoint失败时，首先通过Flink Dashboard查看任务的运行状态和反压情况。如果是全链路反压，可能是下游Sink写入慢或数据倾斜导致某个S...
- 场景三：HDFS存储异常：文件 `sop-006/03_03_hdfs_storage.md`；关键词：p0、p1、故障、异常、恢复、告警、监控、HDFS、NameNode、Active/Standby、fsck、balancer、Spark/MapReduce、Hive；摘要：HDFS告警显示存储使用率过高或数据节点异常时，首先检查NameNode的Active/Standby状态确认高可用切换是否正常。如果是存储空间不足，通过HDFS的fsck命令检查数据分布，使用...
- 场景四：数据质量问题：文件 `sop-006/03_04_data_quality.md`；关键词：异常、处理、告警、监控、指标、质量、ETL、JOIN；摘要：数据质量监控告警显示核心指标异常时，需要判断是数据源问题还是ETL处理问题。首先查看数据源的原始数据量和分布是否正常，再逐级检查ETL各环节的数据转换逻辑。常见的数据质量问题包括：上游系统数据格...
- 场景五：Kafka集群故障：文件 `sop-006/03_05_kafka_cluster.md`；关键词：p0、p1、故障、k8s、延迟、kafka、Kafka、Broker、Leader、ZooKeeper、Controller、Partition、ISR、Follower；摘要：Kafka集群出现Broker下线或Leader选举频繁时，首先确认ZooKeeper集群的健康状态，因为Kafka依赖ZooKeeper进行元数据管理。检查Kafka Controller日志...
