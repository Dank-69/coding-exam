# 三、常见故障处理索引

- 场景一：服务大面积超时：文件 `sop-001/03_01_service_timeout.md`；关键词：p0、p1、故障、恢复、降级、限流、告警、redis、ci、Nacos、Kong、Redis、MySQL、Grafana；摘要：当发现多个服务同时出现超时告警时，首先检查是否有全局性基础设施故障，包括注册中心Nacos是否正常、网关Kong的健康状态、以及底层Redis和MySQL集群的连通性。通过Grafana的全局大...
- 场景二：单服务OOM崩溃：文件 `sop-001/03_02_oom.md`；关键词：p0、p1、排查、回滚、监控、oom、outofmemory、outofmemoryerror、jvm、pod、kubernetes、崩溃、OOM、Java；摘要：Java服务出现OutOfMemoryError时，Kubernetes会自动重启Pod。值班人员需在重启后立即保存堆转储文件用于后续分析。检查最近是否有代码发布或配置变更，查看JVM监控面板确...
- 场景三：数据库连接池耗尽：文件 `sop-001/03_03_db_pool_exhausted.md`；关键词：p0、p1、异常、处理、监控、数据库、连接池、慢查询、MySQL、show、processlist、DBA、Kill；摘要：服务日志出现无法获取数据库连接的错误时，首先通过监控确认连接池使用率。检查是否存在慢查询占用连接不释放的情况，通过MySQL的show processlist命令查看活跃连接。如果确认是慢查询导...
- 场景四：消息队列积压：文件 `sop-001/03_04_kafka_backlog.md`；关键词：p0、p1、升级、延迟、kafka、积压、Kafka、Manager、Partition；摘要：Kafka消费者组出现消费延迟持续增长时，首先确认消费者实例是否正常运行。通过Kafka Manager查看各Partition的消费偏移量，确认是全部Partition积压还是个别Partit...
- 场景五：配置变更导致服务异常：文件 `sop-001/03_05_config_change.md`；关键词：p0、p1、异常、恢复、回滚、Apollo、Nacos、Spring、Cloud、Config；摘要：Apollo或Nacos配置热更新后服务出现异常，第一时间在配置中心查看最近的变更记录，确认变更内容和变更人。如果能明确是某次配置变更导致，立即回滚配置到上一个版本。配置回滚后需要观察至少五分钟...
- 场景六：第三方接口故障：文件 `sop-001/03_06_third_party_api.md`；关键词：故障、降级；摘要：当依赖的外部第三方接口出现超时或错误率升高时，首先确认是否为第三方服务自身故障，可通过其官方状态页面或直接联系对方技术支持确认。在确认第三方故障后，启用本地缓存兜底策略或降级方案。对于支付类接口...
