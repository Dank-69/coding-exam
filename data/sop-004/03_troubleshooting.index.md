# 三、常见故障处理索引

- 场景一：Kubernetes节点NotReady：文件 `sop-004/03_01_kubernetes_node_not_ready.md`；关键词：p0、p1、异常、排查、pod、kubernetes、Kubernetes、NotReady、kubectl、describe、node、kubelet、SSH、systemd；摘要：发现节点变为NotReady状态时，首先通过kubectl describe node查看节点事件和状态条件，确认是kubelet进程异常、网络不通还是资源耗尽导致。如果是kubelet异常，S...
- 场景二：Etcd集群异常：文件 `sop-004/03_02_etcd_cluster.md`；关键词：p0、p1、故障、异常、处理、恢复、告警、kubernetes、数据库、延迟、cd、Etcd、Kubernetes、API；摘要：Etcd是Kubernetes的核心组件，其故障会导致整个集群的API Server不可用。发现Etcd告警时需最高优先级处理。检查Etcd集群成员状态和Leader选举情况。常见问题包括：磁盘...
- 场景三：Ingress网关过载：文件 `sop-004/03_03_ingress_overload.md`；关键词：p0、p1、响应、pod、ab、Ingress、Controller、Nginx、Envoy、Pod、CPU、upstream、Bad、Gateway；摘要：当Ingress Controller（Nginx Ingress或Envoy）出现大量五百错误时，首先查看Ingress Pod的资源使用率。如果是CPU或内存不足，临时增加Ingress C...
- 场景四：CI/CD流水线大面积失败：文件 `sop-004/03_04_cicd_pipeline_failure.md`；关键词：p0、p1、ab、ci、cd、CI/CD、Jenkins/GitLab、Runner、Agent、Docker、Maven、npm、Registry、Daemon；摘要：多个项目的CI/CD流水线同时失败时，首先排除基础设施层面的问题。检查Jenkins/GitLab Runner的Agent节点是否正常运行，构建节点的磁盘空间是否充足，Docker镜像仓库是否...
- 场景五：云资源配额不足：文件 `sop-004/03_05_cloud_quota.md`；关键词：p0、p1、监控、pod、kubernetes、Kubernetes、Pod、vCPU、EIP、API；摘要：当自动伸缩无法创建新实例或Kubernetes无法调度Pod时，可能是云资源配额耗尽。检查当前区域的实例配额、vCPU配额、EIP配额等。通过云厂商控制台或API查看配额使用情况。临时方案包括：...
